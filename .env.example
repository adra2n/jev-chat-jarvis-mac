# jev-jarvis 配置示例
# 变量名都是通用约定。推荐放 ~/.config/jev-jarvis/env（仓库之外，不可能被提交）；
# 放这里（项目根目录 .env）也可以，已在 .gitignore 中排除。

# ── 判断层：TypeSafe Jev（无嘴模型，只输出意图/风险，不写文本）────────────
# 填了就用官方 Jev；**留空则用本地 Laya 模型（离线，模型已在缓存里）**。
# 想确保「零数据外传」就保持留空——start.command 会同时置 HF_HUB_OFFLINE=1。
TYPESAFE_API_KEY=
TYPESAFE_BASE_URL=https://api.typesafe.ai
TYPESAFE_MODEL=jev-latest

# ── 本地判断模型（Laya）───────────────────────────────────────────────
# 默认使用 Laya typed-decisions 模型，无需配置
# JUDGE_BACKEND=laya

# ── 生成层已移除 ────────────────────────────────────────────────────────
# 不生成回复、不调用任何外部端点、面板上也没有候选回复/填入区。
# 因此 OPENAI_* / ANTHROPIC_* / LLM_MODEL / JEV_TONES / JEV_GENERATION
# **没有任何代码读取**，在这里填了也无效；保留这段注释只为提醒别误以为填了就有。

# ── 可选：启动时打开「YOLO 检测框」────────────────────────────────────
# 把每次 OCR 认出的消息用彩框实时框在微信窗口上（对方/我分色+真实置信度，
# 被判断那条带意图·风险标签）。点击穿透、不进抓图。菜单栏 J 图标随时开关。
#JEV_BOXES=1

# ── 桌宠（默认开，JEV_PET=0 关）────────────────────────────────────────
# HUD 旁边矢量画的圆脸小人：8 类意图给表情，风险 0-9 给描边色（绿/琥珀/红）；
# 待机会眨眼呼吸，分析中带「?」，识别把握 <0.5 也带「?」。
# **信息面板默认隐藏，点桌宠才出现**（再点收起，不自动收）；菜单栏 J 图标
# 可整个关掉桌宠——关掉后信息面板自动改成常驻，否则就没入口了。
#JEV_PET=0
